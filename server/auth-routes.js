import express from 'express';import {beginOAuth,takeOAuthState,exchangeCode} from './auth.js';const router=express.Router();const clientId=process.env.DERIV_CLIENT_ID;const redirectUri=process.env.DERIV_REDIRECT_URI||'http://localhost:3000/auth/callback';router.get('/login',(req,res)=>{if(!clientId)return res.status(500).json({error:'DERIV_CLIENT_ID not configured'});res.redirect(beginOAuth({clientId,redirectUri,scope:'trade'}))});router.get('/callback',async(req,res)=>{try{const s=takeOAuthState(req.query.state);const t=await exchangeCode({clientId,code:req.query.code,codeVerifier:s.verifier,redirectUri:s.redirectUri});res.json({ok:true,expires_in:t.expires_in,message:'Authentication completed. Token remains server-side.'})}catch(e){res.status(400).json({error:e.message})}});export default router;